export function sanitizeUser(user){ if(!user) return null; return {_id:user._id?.toString?.()||user.id,id:user.id||user._id?.toString?.(),name:user.name||"",email:user.email||"",photo:user.photo||user.image||"",image:user.image||user.photo||"",role:user.role||"buyer",phone:user.phone||"",location:user.location||"",status:user.status||"active",isVerifiedSeller:Boolean(user.isVerifiedSeller),provider:user.provider||"credentials",createdAt:user.createdAt||null,updatedAt:user.updatedAt||null}; }
